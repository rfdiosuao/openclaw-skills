/**
 * Skill 单元测试
 */

import { main } from '../src/index';

describe('Skill Tests', () => {
  test('should respond to message', async () => {
    // TODO: 实现测试用例
    expect(main).toBeDefined();
  });
});
